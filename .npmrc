auto-install-peers=true
# Motion+ premium patterns are wired up out-of-band via `npx motion-ai` (a
# one-time step that configures the Motion MCP + /motion skill and writes the
# registry auth from MOTION_TOKEN). The blinds transition is hand-rolled, so the
# repo installs cleanly for anyone without the token.
